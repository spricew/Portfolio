---
const { title, Icon } = Astro.props;
---

<div class="flex items-center gap-4">
    {
        Icon && (
            <span class="flex items-center justify-center size-16 rounded-full ring-1 ring-white/30 bg-white/20 backdrop-blur-md">
                <Icon class="inline-block size-10 text-white" />
            </span>
        )
    }
    <h2 class="text-4xl font-semibold text-white font-['Figtree']">
        {title ?? "Sección"}
    </h2>
</div>