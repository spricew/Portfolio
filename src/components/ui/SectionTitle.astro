---
const { title, Icon } = Astro.props;
---

<div class="flex items-center gap-4">
    {
        Icon && (
            <span class="flex items-center justify-center size-10 md:size-14 lg:size-16 rounded-full ring-1 ring-white/30 bg-white/20 backdrop-blur-md">
                <Icon class="inline-block size-6 md:size-8 lg:size-10 text-white" />
            </span>
        )
    }
    <h2 class="text-2xl md:text-3xl lg:text-4xl font-semibold text-white font-['Figtree']">
        {title ?? "Sección"}
    </h2>
</div>