---
const { Icon, schoolName, career, date } = Astro.props;
---

<div class="flex items-center gap-8 px-8 py-6 rounded-4xl bg-primary-900/50 backdrop-blur-md overflow-hidden">
    <Icon class="size-14 text-white" />

    <div class="flex flex-col gap-2">
        <span class="text-2xl font-semibold text-white">{schoolName}</span>
        <p class="text-xl font-medium text-white">{career}</p>
        <span class="text-base text-white">{date}</span>
    </div>
</div>